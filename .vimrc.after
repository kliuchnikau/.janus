vmap <C-S-C> "+y

if has("gui_running")
  set lines=90 columns=200
  set mouse=a "get full mouse support

  syntax enable
  set background=dark
  colorscheme solarized "grb256
else
  set t_Co=256
  syntax enable
  set background=dark
  colorscheme solarized "grb256
endif
